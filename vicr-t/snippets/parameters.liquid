{% if custom_parameters.error > '' -%}{% 	assign ErrorModel = custom_parameters.error --%}{% else -%}{% 	assign ErrorModel = "String" -%}{% endif -%}{% if custom_parameters.model > '' -%}{% assign DomainModel = custom_parameters.model -%}{% else -%}{% assign DomainModel = "<#DataDomainModel#>" -%}{% endif -%}{% if custom_parameters.uikit == 'nk' %}	{% assign ui_imports = "import NavigationKit" -%}	{% assign ui_prefix = "NK" -%}{% else -%}	{% assign ui_imports = "import UIKit" -%}	{% assign ui_prefix = "UI" -%}{% endif -%}{% if custom_parameters.base > '' %}	{% assign ui_prefix = custom_parameters.base -%}{% endif -%}{% if custom_parameters.type == 'table' -%}	{% assign ViewClass = ui_prefix | append: "TableViewController" -%}{% else -%}	{% assign ViewClass = ui_prefix | append: "ViewController" -%}{% endif -%}